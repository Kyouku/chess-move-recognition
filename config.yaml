paths:
  video_dir: ../data/raw
  calibration_dir: ../data/calibration
  results_dir: ../results
  model_path: ../models/yolov8n.pt
  ground_truth: ../data/labels/game1_truth.json

pipeline:
  frame_step: 5
  confidence_threshold: 0.5
  image_size: 640
  rectified_board_size: [ 800, 800 ]
  save_visualizations: true

evaluation:
  compare_method: "index"
